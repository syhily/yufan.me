---
import { Image } from 'astro:assets'
import _ from 'lodash'
import { friends } from '@/helpers/content/schema'

const list = _.shuffle(friends)
---

{
  list.length > 0 ? (
    <div class="list-bookmarks px-3 md:px-0">
      <h2 class="text-muted mb-4 md:mb-3">
        左邻右舍 <span class="text-primary text-sm mb-4 md:mb-3">排名不分前后</span>
      </h2>
      <div class="flex flex-wrap gap-3 md:gap-4 list-grouped">
        {list.map((friend) => (
          <div class="w-full md:w-4/12">
            <div class="list-item block">
              <div class="media media-3x1">
                <div class="media-content">
                  <Image src={friend.poster} alt={friend.website} width={1280} height={425} />
                </div>
              </div>
              <div class="list-content">
                <div class="list-body">
                  <div class="list-title h6 h-1x">{friend.website}</div>
                  <div class="text-sm text-secondary h-2x mt-1">{friend.description ? friend.description : ' '}</div>
                </div>
              </div>
              <a href={friend.homepage} target="_blank" class="list-gogogo" />
            </div>
          </div>
        ))}
      </div>
    </div>
  ) : (
    <div class="data-null">
      <div class="my-auto">
        <div>还没有友链呢...😭</div>
      </div>
    </div>
  )
}
