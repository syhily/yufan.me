---
import { generateToken } from '@/helpers/auth/csrf'

const session = Astro.session
const token = session ? generateToken(session) : 'Please configure your astro session'
---

<input type="hidden" name="token" value={token} />
