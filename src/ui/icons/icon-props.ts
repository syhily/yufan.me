export interface IconProps {
  /** CSS sizing hint; defaults to `1em`. */
  size?: string | number
  /** Accessible title; if set the icon is exposed as `role="img"`. */
  title?: string
  className?: string
}
