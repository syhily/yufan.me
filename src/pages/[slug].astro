---
import { getPage } from '@/helpers/content/schema'
import PageLayout from '@/layouts/PageLayout.astro'

const { slug } = Astro.params
if (!slug) {
  return Astro.redirect('/404')
}

const page = getPage(slug)
if (!page) {
  return Astro.redirect('/404')
}
---

<PageLayout {page} />
