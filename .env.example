# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# Site
LOG_LEVEL=

# Optional: allow fetching missing image metadata from the CDN during MDX compile / SSR (default off; use `npm run image:metadata:sync` instead).
# IMAGE_METADATA_REMOTE_FALLBACK=1

# Server
HOST=0.0.0.0
PORT=4321

# Database
DATABASE_URL=
REDIS_URL=

# Session cookie signing
SESSION_SECRET=

# CDN host for committed images / music / avatars. The MDX compile pipeline
# and the `sync-image-metadata` CLI read these values directly (they run
# before any DB connection exists). The runtime (SSR + client) reads the
# same values out of the DB-backed settings document — keep both layers in
# sync at deploy time.
ASSET_HOST=cat.yufan.me
ASSET_SCHEME=https
